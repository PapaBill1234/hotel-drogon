# Current parity and release-gap inventory

Updated 2026-09-26 for the website-enforced SSO ticket lifetime, with the latest local evidence below. This file tracks the Drogon/React application. The former 50 KB inventory, including the Laravel attempt and the investigation diary, is preserved verbatim in [the v1 archive](archive/phase1-parity-inventory-v1.md) and in Git history. The [stable plan](cpp-drogon-conversion-plan.md) owns architecture and safety rules; [run state](ai-run-state.md) owns the active unit.

**Status terms:** *Verified* means the cited check passed for its stated scope; *implemented* means code exists without the complete user journey; *gap* means no working current-stack path; *decision gate* means the named approval or emulator evidence is required. A passing phase exit does not imply release readiness.

## Ownership and source of truth

| Data family | Owner and rule | Source/evidence |
| --- | --- | --- |
| PolarIS hotel `users`, guilds/members, rooms, inventory, catalog, balances, bans, and hotel permissions | PolarIS. Keep its schema unchanged absent a verified emulator extension. Reads and writes go through individually authorized, audited named service methods; no generic table write or raw controller SQL. | Plan rules 1, 3–4; `src/services/{UserAccount,Guild,Ban,Report}Service.cpp`; `scripts/check_polaris_access.py` |
| Website `phpretro_*` content, reports, help desk, homes data, and audit tables | Website/CMS, using the same named-service access style and prepared statements. | `src/services/ContentService.cpp`; plan stack and Phases 4, 8–9 |
| Historical Laravel `sys`/Fortify identity | Superseded; it is not the hotel login in this repository. | [v1 archive](archive/phase1-parity-inventory-v1.md) |

The read-only PHPRetro PHP implementation is the behavioral reference before any feature work. This workspace now has the sibling `legacy/phpretro-pdo` checkout used for local verification; a fresh clone still needs that external checkout to supply legacy assets.

## Foundation and delivery

| Feature | Current status | Source and evidence | Gap / next step |
| --- | --- | --- | --- |
| C++ backend, worker binary, MariaDB, Redis, nginx | **Verified for existing scope.** Pinned Ubuntu 24.04/Drogon 1.8.7 build; sanitizers, `-Werror`, CTest, live integration, and isolated cutover passed with curl at `8.5.0-2ubuntu10.15` in both Dockerfile stages. | `Dockerfile`, `CMakeLists.txt`, `compose.yaml`; [run 36224550725](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36224550725) passed all three jobs. The strict pin checker was not changed. | Fresh-clone legacy asset acquisition is external. |
| Development and verification loop | **Measured for this host and code baseline.** Cold/warm clean C++ builds took 41.82/37.07 s, one `.cpp` edit 7.07 s, shared-header edit 26.53 s, and each link under 0.2 s. A docs-only Docker rebuild fell from 38.82 to 2.86 s after filtering non-build inputs. | [Phase 2b measurements](phase2b-build-loop-measurements.md); `.dockerignore` keeps docs, frontend dependencies, generated files, and local state out of the backend context. The image, CTest, sanitizer build, security lints, isolated smokes, browser flows, and cutover were rechecked for this work unit. | No extra build tool is justified by this single baseline. |
| Phase 2b frontend delivery | **Verified.** Compose builds from the locked Node 20 frontend image, copies the bundle to a generated volume, and starts nginx only after a successful copy. No host `frontend/dist` build is required. | `frontend/Dockerfile`, `frontend/.dockerignore`, `compose.yaml`, `.github/workflows/ci.yml`; isolated fresh-volume proxy served the shell and hashed JavaScript, and [run 36224550725](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36224550725) passed the Compose delivery assertion and browser checks from a fresh checkout. | Legacy static assets still require the read-only sibling checkout. |
| Worker health | **Verified configuration.** The worker has no HTTP listener, so Compose disables the inherited server probe and reports its running state without a health claim. | `compose.yaml`; isolated worker was `running` with no health object; [run 36224550725](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36224550725) asserted the same on the root stack. | Add a real worker probe only when there is meaningful work to test. The untouched primary container still has its old unhealthy state until redeployment. |
| Legacy static assets | **Conditional.** Six public pages depend on legacy `web-gallery` and admin images; CI fetches them, and this workspace has a read-only sibling checkout. | `compose.yaml` sibling mounts; `.github/workflows/ci.yml` legacy fetch and guard; `proxy/nginx.conf`. | Document fresh-install acquisition and check rights before redistribution. |
| OpenAPI account slice | **Verified for implemented routes.** Seventeen paths and nineteen explicit methods cover auth, `/api/me`, profile, credits, recovery, remember-me, reauthentication, and client entry. The client-entry mutation is now a CSRF-protected POST. The contract records current error/status, cookies and validation, including unsafe staff 2FA semantics. | `docs/openapi-account-v1.json`; `scripts/check_account_contract.py` resolves references, compares controller macros, validates response shapes, and passed **1207 live assertions** on the disposable `ci-remember-diagnostic-20260926` stack. | Unsupported operations are not specified as working. |
| Cutover and rollback | **Verified for one test route.** `/articles/rss.xml` switches to legacy and back through the real map in an isolated stack. | `scripts/check_cutover.sh` 8/8 locally and `cutover-check` in [run 36224550725](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36224550725). | Full route-by-route cutover is future Phase 10 work. |
| Metrics and error reporting | `/metrics` implemented; Sentry not wired. | `include/controllers/HealthController.h`; plan Phase 10 gate. | Decide Sentry source or record an observability gap at Phase 10. |

## Current public, CMS, and account surface

| Feature | Current status | Source and evidence | Gap / next step |
| --- | --- | --- | --- |
| Public content API and RSS | **Verified for current routes.** Landing, news, community-news, FAQ, collectables, banners, campaigns, maintenance, settings, and RSS. The legacy RSS title double-escape is intentionally corrected. | `include/controllers/PublicContentController.h`; `scripts/smoke_phase4_public.py` recorded 21/21 locally; `integration-smoke` job. | Extend only with a verified legacy behavior read. |
| Six public React pages | **Verified for measured parity, with one page now content-asserted.** Landing, community, articles, help, collectables, maintenance: 6/6 captured legacy baselines within 2%. The `/community` promo additionally has a DOM-vs-API assertion, because a 2% pixel budget is larger than a missing paragraph — see the row below. | `frontend/src/App.tsx`; `docs/reference-screenshots/baseline/`; `tests/e2e/visual-parity.spec.ts`; 6/6 in the pinned image with `site_closed=1` (the maintenance baseline's precondition). | This does not establish admin visual fidelity or a complete product. The `collectables` baseline is misaligned — it shows the **Retro Club** tab active while the spec captures `/credits/collectables` — and diffs ~3% on some runs. |
| `/community` "Latest news" promo | **Fixed and verified.** The promo read the wrong table. `community.php:372` reads **`hotelview_news`**; the React page read `phpretro_news` and, once that list ran short, padded five blank slots — so the topstory rendered an empty blue box against a populated legacy page. Now served by `GET /api/public/community-news`, shaped by `buildPromoNews` (HoloText + nl2br + HoloUrl + `stringToURL` slug, exactly as the PHP did). The legacy table's ship row is created and seeded by `ContentService::ensureSchema`. | `src/services/ContentService.cpp` (`hotelview_news` CREATE + seed, `listHotelviewNews`); `src/controllers/PublicContentController.cpp`; `frontend/src/services/communityNews.ts`; `frontend/src/pages/CommunityPage.tsx`. Verified: endpoint returns the legacy row; `visual-parity.spec.ts` community green against the real baseline; the new guard was shown to fail on the pre-fix behaviour (`promo slot for "Open Your Summer Calendar!" did not render its title`); smoke 21/21. | `hotelview_news` is read-only on the public surface: legacy shipped its rows with the schema dump and had **no** admin page writing them, so there is no admin CRUD to port yet. Add one only alongside a verified legacy writer. |
| `/community` "Random Habbos" widget | **Open visual divergence (not the news bug).** `#placeholder-container` computes to `position: static` with **height 0**, so all 18 `.active-habbo-image-placeholder` divs anchor to `#homes-habblet-list-container` and stack at its origin. The imagemap covers 176,343–611,573, but the container is 450x242 ending at y=585, so the 64x110 `habbo_skeleton.gif` spills ~12px below the blue box. Legacy likewise emits 18 placeholders, but its `fullcontent.js` `ActiveHabbosHabblet.initialize()` assigns each one grid coordinates (3 rows x 6 columns, 62x45 spacing); with no live data `generateRandomImages` never repaints them. | Measured live: `#placeholder-container` static 450x0 at 176,575; `elementFromPoint(180,590)` = a `DIV.bb` (`boxblue.png`). Baseline at the same point shows nothing. `web-gallery/static/js/fullcontent.js` `ActiveHabbosHabblet`. | Port `_positionPlaceHolderImages`, or omit the placeholders when there is no habbo data. Worth <1% of pixels, which is why the pixel budget did not surface it — found from a user screenshot. |
| Phase 4 CMS API | **Verified for existing content resources.** News, FAQ, collectables, banners, campaigns CRUD and settings; high-trust raw HTML gate. | `include/controllers/AdminContentController.h`; `scripts/smoke_phase4_admin.sh` recorded 33/33; `scripts/check_admin_ui_coverage.py`. | Broader Phase 9 staff resources are separate. |
| Phase 4 React content panel | **Verified for its narrow flows.** Dashboard/login plus news, FAQ, banners, campaigns, collectables, settings; 10/10 browser assertions. | `frontend/src/App.tsx`; `tests/e2e/admin.spec.ts`; `integration-smoke` job green. Coverage lint intentionally excludes diagnostic `test-gate` and Phase 9 bans routes, so “every admin route” is too broad. | Compare actual legacy admin screens with user expectations, review keyboard use, record intentional differences, and fix material issues. |
| Public auth/session and `/api/me` | **Verified for narrow Phase 3 checks.** Login, logout route, Redis-backed public/staff cookies, rank gate, CSRF and named PolarIS access layer. | `include/controllers/AuthController.h`; `scripts/smoke_phase3.sh` recorded 12/12; `scripts/check_csrf_rules.py` and `scripts/check_polaris_access.py` in green CI. | Narrow smoke does not prove all account journeys or staff 2FA. |
| Login throttling | **Gap.** `UserAccountService::authenticate` has no attempt counter, lockout or backoff; repeated wrong passwords are answered as fast as correct ones. The plan lists login throttling among the things the custom session service must implement independently. | `src/services/UserAccountService.cpp` (no throttle/rate-limit reference); `tests/e2e/account.spec.ts` drives several failed logins per run without being rate-limited. | Implement Redis-backed attempt counters with a tested lockout window. Until then, do not describe login as hardened. |
| Staff step-up / TOTP | **Release gap.** Separate rank-gated staff session exists, but staff login marks 2FA verified with no code and checks only six-character length if a code is supplied; no enrolled secret is read. | `src/controllers/AuthController.cpp` staff login; `src/utils/Crypto.cpp` has a TOTP verifier that this path does not use. | Verify legacy/emulator enrollment data, implement and test real per-staff secret validation before staff release. |
| Supported profile edits | **Verified through React.** Motto, figure, gender, email and password are editable at `/account/profile`, each through a named service method with CSRF. Validation matches legacy `profile.php`: over-long values are **rejected** with its "Invalid profile details." notice and inputs are trimmed before measuring and storing. | `frontend/src/pages/account/ProfilePage.tsx`, `include/controllers/AccountController.h`, `src/controllers/AccountController.cpp`; `tests/e2e/account.spec.ts` 18/18; `scripts/check_account_contract.py` asserts the 127/255 boundaries, trimming and gender rejection against the live routes. | None for these fields. `users.look` is `varchar(255)` and `users.gender` is `enum('M','F')`, so the handler's 255 bound and gender rejection express the schema rather than a policy choice. |
| React account journey (sign in, `/me`, profile, sign out) | **Verified for the existing-user slice.** `/account` signs in and issues both public cookies, `/me` renders the live PolarIS row (username, motto, credits, pixels, last login), `/account/profile` edits the fields above, `/logout` ends the session. A signed-out visitor to a guarded page gets the sign-in form *in place*, with no redirect loop. | `frontend/src/pages/account/{LoginPage,MePage,ProfilePage,LogoutPage}.tsx`, `frontend/src/components/{AccountPage,CommunityShell}.tsx`, `frontend/src/services/apiAccount.ts`; `tests/e2e/account.spec.ts` **18/18** covering success, wrong password, wrong current password, over-long and out-of-enum input, logout, an expired session cookie, anonymous API calls and a missing CSRF token. | Visual fidelity of these pages is **not** measured: no screenshot baseline exists for a signed-in page, and the suite is functional. `mail_verified`, credits/history, Club and the client handoff are separate rows. |
| Credits balance read (`/credits`) | **Verified through React.** `/credits` renders the purse from the live PolarIS row (`/api/account/purse` → `credits`, `pixels`, `points`), reusing `credits.php`'s own `#purse-habblet` markup and its `/credits/history` link. | `frontend/src/pages/account/WalletPage.tsx`, `include/controllers/CreditsController.h`; `tests/e2e/credits.spec.ts` 7/7; `scripts/check_account_contract.py` asserts the balances equal `/api/me`'s. | The legacy page's `#credits-methods` and promo blocks are the file's own sample copy ("THIS IS A SAMPLE HABBLET ONLY!"), so they are tenant content and are not ported. |
| Transaction history read (`/credits/history`) | **Verified through React.** The converted `history.php`: the signed-in user's own `phpretro_transactions` rows, `created_at DESC, id DESC`, capped at 100, rendered as Date/Type/Amount/Balance/Description with the legacy date format and the legacy empty-state sentence. | `frontend/src/pages/account/TransactionHistoryPage.tsx`, `include/services/TransactionService.h`, `src/services/TransactionService.cpp`, `src/main.cpp` (schema); `tests/e2e/credits.spec.ts` 7/7 asserts both legacy row shapes and the ordering; `tests/unit/TransactionServiceTest.cpp` covers the service's own validation. | **The ledger has no writer in this stack yet.** Only housekeeping's credit adjustment (`admin_grant`, Phase 9) and the MyHabbo Homes store (`homes_store`, Phase 8) ever wrote rows, so a normal stack shows the empty state — parity with a fresh legacy install, verified by seeding rows in the browser suite rather than by pretending they exist. |
| Client entry (`/client`) | **Verified in a loopback-only CMS/PolarIS/Octane lab, with the ticket lifetime enforced by the website.** A CSRF-protected `POST /api/account/client-entry` rotates the signed-in user's legacy-format SSO ticket through the audited named service and reports `sso_ticket_void_at`. At that deadline `voidExpiredAuthTickets` (a 5 s Drogon timer) replaces the ticket with a value the client cannot present, and sign-out voids it immediately. The tombstone — not an empty column — is what makes the void final, because the pinned emulator restores a consumed ticket into an empty column only. | `tests/e2e/sso-replay.spec.ts` **6/6** in the lab: login, reconnect inside the window, full disconnect, disconnect *after* the void, sign-out, past its window — each replay path asserted against the emulator's SSO endpoint *and* the client handshake. The same suite failed 3 of 5 paths before the change (HTTP 200 where 401 was required). `scripts/check_sso_replay_bound.sh` asserts it on the row in CI; `scripts/check_account_contract.py`; `tools/emulator-lab/compose.cms.yaml`. | The window is the residual exposure: a captured ticket is replayable inside `SSO_TICKET_TTL_SECONDS` (default 120 s), and a reconnect after the window needs a fresh launch from `/client`. Nothing here bounds a ticket the *emulator* issues, and an emulator revision that restored a ticket unconditionally would defeat the tombstone, so the pin must be re-verified with this suite when it moves. A non-loopback (HTTPS) origin is a separate follow-up. |
| Voucher redemption | **Unsupported until verified emulator integration.** No route and no form: `credits.php` posted `voucherCode` to itself, and rendering a disabled input with no handler would be the "control that silently no-ops" the plan forbids. This matches the legacy codebase's own `client-handoff` convention (`docs/phase-reports/client-handoff.md`), which made the same call for voucher redeem, Club buy and the badge editor. | `frontend/src/pages/account/WalletPage.tsx`; plan Phases 5 and 10. | Explicit handoff. |
| Credits/history and Nitro client entry | **Existing-user journey verified in the disposable lab, now with a bounded ticket.** CMS sign-in, account reads, `/client` ticket handoff, Octane room rendering, and a website-enforced ticket deadline each have browser evidence. | `tests/e2e/{credits,client,cms-octane,sso-replay}.spec.ts`; `tools/emulator-lab/README.md`. | This is a development loop, not production readiness; asset rights, reset mail delivery, signed-in visual baselines and an HTTPS client origin remain gaps. |
| Password recovery (`/account/password/forgot`) | **Implemented; delivery is a recorded gap.** Both legacy `forgot.php` actions are ported: password recovery (username **and** email exact, `mail_verified = '1'`) and username recovery (email only). A match issues a single-use token — SHA-256-hashed in Redis with a 30-minute TTL, no PolarIS column added — and the reset page at `/account/password/reset` spends it and sets a new password. | `frontend/src/pages/account/ForgotPasswordPage.tsx`, `include/services/{MailService,UserAccountService}.h`, `src/services/UserAccountService.cpp`; `tests/e2e/password-reset.spec.ts` **10/10**; `scripts/check_account_contract.py` asserts the neutral answer, the single-use rejection and the transport report. | **No mail transport exists** — no SMTP client, no Mailpit/MailHog, no mail setting — so delivery is behind `MailService` and the only transport logs, reporting `logged`, never `delivered`. A real transport is a dependency decision under plan rule 9, not yet taken. The UI states this rather than implying an email was sent. |
| Step-up / reauthentication (`/account/reauthenticate`) | **Implemented and now reachable in normal use.** The legacy `$_SESSION['reauthenticate']` flag is carried in the Redis session as `reauth_required`, reported by `/api/me` and `/api/account/session`, and enforced by the account guard: a session carrying it shows the step-up screen instead of any page content. Clearing it re-runs the full credential check through the same service method login uses. | `frontend/src/pages/account/ReauthenticatePage.tsx`, `frontend/src/components/AccountPage.tsx`, `include/controllers/SecurityController.h`, `src/services/SessionManager.cpp`; `tests/e2e/remember-me.spec.ts` covers the restored-then-stepped-up journey end to end. | Nothing further: remember-me is what sets the flag, and it is now ported. |
| Remember-me | **Verified through React.** Ticking the header's "Remember me" checkbox issues the legacy cookie pair (`rememberme` + `rememberme_token`, names taken from `classes.php`) and stores only the token's SHA-256 digest in `users.remember_token_hash`, with the lifetime from `site_cookie_time` (installer default 14 days). A guarded page holding a live token restores the session — which **always requires step-up** — and signing out clears the stored digest, not just the cookies. | `frontend/src/components/AccountPage.tsx` (the restore), `frontend/src/components/CommunityShell.tsx` (the checkbox), `include/controllers/AuthController.h`, `src/controllers/AuthController.cpp`; `tests/e2e/remember-me.spec.ts` **6/6**; `tests/unit/RememberMeTest.cpp` pins the 6-20-20 token shape and the day defaulting; `scripts/check_account_contract.py` asserts the cookie pair, the digest width, single use and the mandatory step-up. | No gap for this slice. The legacy `type=token` branch that **set** the step-up flag is now ported, so `reauth_required` arises in normal use rather than only from a seeded test. |
| Registration | **Decision gate.** No current registration route or React journey. | Controller route inventory; plan Phase 5. | Approve and test the audited named PolarIS `users` write path before enabling. Until then report unsupported. |
| Club purchase, vouchers, badge/room actions | **Unsupported until verified emulator integration.** | Plan Phases 5, 7, 10; no matching current React journey. | Explicit handoff/gap, never a simulated success. |

## Later feature families and release

| Family | Current status | Source/evidence | Required next slice or gate |
| --- | --- | --- | --- |
| Minimail, friends, presence, WebSockets | **Gap in current Drogon/React UI/API.** | `frontend/src/App.tsx` and controller route inventory. | Minimail list/read/send with ownership/spam tests first; friend requests/presence and real event push later. |
| Groups and discussions | **Gap beyond service groundwork.** | `src/services/GuildService.cpp` exists; no matching React pages or routes. | Verify guild/forum behavior and implement authorized vertical slices. |
| User and group Homes | **Gap.** | No Homes controller or React editor in current tree. | Verify legacy model, approved version/conflict contract, user Homes before group Homes. |
| Remaining housekeeping | **Gap.** Bans API groundwork exists, but no bans UI; user management, alerts, reports, logs, vouchers and other supported actions are not current panel flows. | `include/controllers/StaffTestController.h`; `frontend/src/App.tsx`; `scripts/check_admin_ui_coverage.py` exclusions. | Named authorized/audited methods, server-side confirmations, UI and failure tests per slice; real staff 2FA gate. |
| Release/cutover | **Not met.** | Plan Phase 10 and the narrow evidence above. | Fresh install, recovery, rights, UI acceptance, performance, supported-operation list, independent security review, Sentry decision, then PHP retirement. |
| Browser-native Flash assets | **Separate later milestone.** | Plan milestone 6. | Inventory/decompile SWFs; HTML5 without Ruffle; Trax save/play only with verified PolarIS/Nitro capability. |
| Browser-native hotel client | **Verified in an isolated local lab, now connected to the disposable CMS, with a website-enforced ticket lifetime.** Pinned Octane, Octane-Renderer and PolarIS use local-only Nitro assets mounted read-only. Both an emulator-issued ticket and a Drogon-issued ticket entered a rendered room without Octane's login/registration form; the Drogon ticket is voided by the website at its deadline and cannot be replayed afterwards. | `tools/emulator-lab/`, `tests/e2e/{emulator-lab,cms-octane,sso-replay}.spec.ts`; pinned Playwright 1.63.0 passed, and the emulator logged room entry and session parking. | A non-loopback HTTPS origin, asset redistribution rights, and one camera effect image remain gaps. |

## Phase exits and next unit

| Phase | Accurate state |
| --- | --- |
| 1 | **Exit met:** current inventory and the first OpenAPI slice match implemented routes; the isolated live contract check passed 676 assertions and all three CI jobs passed in [run 36226068832](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36226068832). |
| 2 | Repository skeleton and legacy reference were established in earlier work; this workspace now has the sibling assets for local verification, while fresh installation still needs acquisition instructions. |
| 2b | **Narrow exit met:** the measured and filtered build loop, warning-free sanitizer compilation and CTest, Compose-built React delivery, truthful worker state, live integration/browser checks, real route cutover/rollback, and all three jobs of [run 36224550725](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36224550725) passed. The primary stack was not recreated; its old worker container still reports unhealthy. |
| 3 | **Narrow exit met:** 12/12 smoke, staff rank rejection and service isolation. Password reset and remember-me have since gained separate verified React flows; real staff TOTP remains a release gap. |
| 4 | **Narrow exit met:** current CMS flows in React and six public screenshot comparisons. Admin appearance/usability remains an outcome/release review. |
| 5–10 | Later feature families and release gates are not complete. **Phase 5's existing-user journey now reaches a rendered Octane room in an isolated lab through a Drogon-issued ticket whose lifetime the website enforces**, so a ticket captured from history or logs is not replayable after its window. Website account/profile/credits and reset/remember paths retain their separate browser coverage. This local result does not meet the whole Phase 5 exit for release: mail delivery, signed-in visual review, the registration decision gate and a non-loopback client origin remain. |

**Exact next work unit:** Review an HTTPS (non-loopback) Octane origin for the client launch now that the replay bound is verified: the origin allowlist in `octaneClientUrl`, TLS termination, and the cookie/`Secure` consequences of leaving loopback. Keep the primary hotel and legacy databases separate from the lab, and keep Octane's standalone login disabled.

**Following unit:** Registration remains a Phase 5 decision gate: a new account may be written to PolarIS `users` only after an explicit recorded decision to trust a named, audited `UserAccountService::registerAccount` path. Without that decision, proceed to Phase 6's minimail list/read/send slice. Signed-in page visuals, mail transport, ledger writers, and staff TOTP remain separate gaps.

### SSO ticket lifetime carried by this unit

The pinned emulator is not the place this project can fix the ticket lifetime: `POLARIS_REV` is pinned, `fetch-upstreams` refuses a dirty checkout, and its game login deliberately matches `users.auth_ticket` without consulting `auth_ticket_expires_at` (its own comment records that a stale expiry used to block third-party CMSes that write only that column). So the issuer bounds the credential:

- `phpretro_sso_tickets` (website-owned, one row per user) holds the ticket, `issued_at`, `void_at` and `voided_at`. It is **not** in Redis: a deadline lost to a cache restart would be exactly the unbounded credential this prevents.
- At the deadline a 5 s sweep writes a tombstone with `auth_ticket = ? WHERE id = ? AND (auth_ticket = ? OR auth_ticket = '')`. The `= ''` arm reaches the state a consumed ticket leaves behind, which is the state the emulator's grace-period restore writes back into — clearing to empty would hand the dead ticket straight back to whoever holds the URL.
- The tombstone (`void-` + 190 hex) is longer than the 128 characters both PolarIS doors accept as a *presented* ticket, so it is not a credential even if it leaks.
- `SSO_TICKET_TTL_SECONDS` (default 120, clamped 5–3600) and `SSO_TICKET_SWEEP_SECONDS` (default 5) are the operator knobs. The window must exceed a client launch; the lab overlay uses 90 s and the CI stack 10 s so the void can be observed inside a job.

**A pre-existing crash on this route was found and fixed.** Ten consecutive `POST /api/account/client-entry` calls killed the backend with the `Exited (139)` signature already recorded in the run state. AddressSanitizer named the fault: the four parallel `phpretro_site_settings` lookups complete on different database loop threads, and their shared `std::map` and `remaining` counter were unsynchronized — two callbacks inserted into one map at once (tree corruption, SIGSEGV), and the counter's race could answer a request twice or never. Both are now guarded by a mutex, with the last callback copying the values out under the lock. Measured: before, a 100-request loop killed the release build at request 8 and the ASan build at request 10; after, 100/100 release and 120/120 ASan, both healthy. CI asserts the bound on the row in every integration run (`scripts/check_sso_replay_bound.sh`), and all three jobs passed in [run 36258890816](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36258890816) on the branch and again in [run 36265374430](https://github.com/PapaBill1234/hotel-drogon/actions/runs/36265374430) on `main`, which the branch was merged into (fast-forward to `ac006ff`; PR [#1](https://github.com/PapaBill1234/hotel-drogon/pull/1) closed as merged). The first of those runs existed only because the conflicted pull request was merged up first — a conflicted pull request gets no workflow runs at all.

### Schema correction carried by this unit

`users.auth_ticket` was declared `VARCHAR(255)` in `src/main.cpp`; PolarIS declares `varchar(256)`, and the legacy site wrote a 36-character ticket there. It is now `VARCHAR(256)`. The older CleanDB evidence lacked `auth_ticket_expires_at`, so this repository removed that column from its own initial DDL and ticket writer. The newly pinned PolarIS emulator has verified migrations that **add** `auth_ticket_expires_at` for its own HTTP issuer. Its game SSO lookup ignores the expiry column. It consumes a ticket after successful login, but `SessionResumeManager` restores it during disconnect grace and leaves it present after the grace expires, as observed in the lab. The two schema baselines must not be conflated.

**An existing database may keep its old column shape** — `CREATE TABLE IF NOT EXISTS` does not alter it. After identifying the database owner and installed PolarIS revision, an older website-only schema may need the reviewed width change:

```sql
ALTER TABLE users MODIFY auth_ticket VARCHAR(256) DEFAULT '';
```

No `DROP COLUMN auth_ticket_expires_at` is prescribed for the pinned emulator; it requires that column. No primary database migration was run in this lab work unit.
