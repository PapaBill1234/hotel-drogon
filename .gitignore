# Build artifacts
build/
out/
# The pinned-container runner mounts its artefacts here (`tests/e2e/.out`).
# Dot-prefixed, so the `out/` rule above does NOT match it -- it showed up as
# untracked after every local parity run until it was listed explicitly.
tests/e2e/.out/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
CMakeScripts/
Testing/
*.o
*.obj
*.exe
*.dll
*.so
*.dylib
*.a
*.lib
*.pdb
*.ilk
*.idb
compile_commands.json

# vcpkg
vcpkg_installed/

# Environment / secrets — real .env files must NEVER be committed
.env
.env.*
!.env.example

# IDEs
.vs/
.vscode/
.idea/
*.swp

# OS
Thumbs.db
.DS_Store

# Frontend
node_modules/
# Anchored on purpose. An unanchored `dist/` matches *any* directory named dist,
# including `frontend/dist`, and an ignored directory is terminal: git never
# descends into it, so `frontend/.gitignore` cannot re-include a placeholder
# inside it. `frontend/dist` must stay in the tree because compose.yaml
# bind-mounts it (see frontend/.gitignore).
/dist/
frontend/node_modules/

# Legacy reference
legacy/

